#include <iostream>
#include <stack>

using namespace std;

/* Stack FILO
 * stack<int> s; STL 六大组件之一 适配器 默认用deque实现
 * s.top() 如果栈已经为空 top方法会造成段错误
 * s.empty() -> bool
 * s.size()
 * s.push(x)
 * s.pop()
 * */

int main(){
	return 0;
}
