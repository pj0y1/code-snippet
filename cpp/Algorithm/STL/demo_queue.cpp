#include <iostream>
#include <queue>

using namespace std;
/* Queue 也是适配器
 * queue<int> q;
 * q.back()
 * q.front()
 * q.push()
 * q.pop()
 * q.size()
 * q.empty() -> bool
 * /
int main(){
	return 0;
}
